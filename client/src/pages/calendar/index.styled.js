import styled from "styled-components";

export const StyledCalendarContainer = styled.main`
  display: flex;
  flex-direction: column;
  position: relative;
  min-height: 100%;
`;
export const StyledCalendarButtonWrapper = styled.article`
  position: absolute;
  top: 0;
`;
